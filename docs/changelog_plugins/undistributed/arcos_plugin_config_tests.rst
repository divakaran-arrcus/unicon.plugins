--------------------------------------------------------------------------------
New
--------------------------------------------------------------------------------
* ARCOS
    * Added mock-based unit tests for the config-mode statemachine transition
      and the Configure service (with and without auto-commit); extended the
      arcos mock device data with a config state.
